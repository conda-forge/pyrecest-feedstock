context:
  name: pyrecest
  version: "2.2.1"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/FlorianPfaff/pyrecest/archive/refs/tags/${{ version }}.tar.gz
  sha256: 7f2fff07ef00cd1269e0d1e4256ff821fb35126c5094cd0c8a41c0ae105c7461

build:
  noarch: python
  script: python -m pip install . -vv
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - poetry-core
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy >=1.16.3
    - matplotlib-base
    - mpmath
    - bayesian-filters
    - pyshtools
    - beartype
    - shapely

tests:
  - python:
      imports:
        - pyrecest
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/FlorianPfaff/pyRecEst
  summary: 'Framework for recursive Bayesian estimation in Python.'
  description: |
    pyRecEst is a Python library tailored for recursive Bayesian estimation, compatible with numpy, pytorch, and jax backends.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - claudiushaag
