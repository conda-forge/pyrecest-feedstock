context:
  name: pyrecest
  version: "2.1.0"
  python_min: "3.11"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/FlorianPfaff/pyrecest/archive/refs/tags/${{ version }}.tar.gz
  sha256: c7c93edfd64a228e3d1f626f6b43f7b4e346d1cbdeb46b625781ff67ab0da5d8

build:
  noarch: python
  script: python -m pip install . -vv
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - poetry-core
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy >=1.16.3
    - matplotlib-base
    - mpmath
    - bayesian-filters
    - pyshtools
    - beartype
    - shapely

tests:
  - python:
      imports:
        - pyrecest
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/FlorianPfaff/pyRecEst
  summary: 'Framework for recursive Bayesian estimation in Python.'
  description: |
    pyRecEst is a Python library tailored for recursive Bayesian estimation, compatible with numpy, pytorch, and jax backends.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - claudiushaag
